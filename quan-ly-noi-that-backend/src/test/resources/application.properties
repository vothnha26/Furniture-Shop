# Test overrides so tests can create missing schema and avoid placeholder failures
spring.jpa.hibernate.ddl-auto=update

# Fallbacks for local/test
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:5173}
application.security.jwt.secret-key=${APPLICATION_SECURITY_JWT_SECRET_KEY:backend-secret-dev-seed-1234567890123456}
